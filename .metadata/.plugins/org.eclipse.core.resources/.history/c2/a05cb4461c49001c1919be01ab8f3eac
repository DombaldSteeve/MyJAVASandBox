package TP14;

public class Main {
	public static void main(String[] args) {
		Figure f = (Figure) FigureUtil.getRandomFigure();
		f.affiche();
		f = (Figure) FigureUtil.getRandomFigure();
		f.affiche();
		f = (Figure) FigureUtil.getRandomFigure();
		f.affiche();
		f = (Figure) FigureUtil.getRandomFigure();
		f.affiche();
		f = (Figure) FigureUtil.getRandomFigure();
		f.affiche();
		f = (Figure) FigureUtil.getRandomFigure();
		f.affiche();
		f = (Figure) FigureUtil.getRandomFigure();
		f.affiche();
		f = (Figure) FigureUtil.getRandomFigure();
		f.affiche();
	}
}
